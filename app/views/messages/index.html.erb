<!DOCTYPE html>

<h1>メッセージ一覧</h1>

<ul>
  
<% @messages.each do |message| %>

<li>
    <!--link_to 表示文字列, リンク先-->
<%= link_to message.id, message %>
 : 
 <%= message.title %>
 > 
<%= message.content %>
</li>
  
<% end %>

</ul>
<%= paginate @messages %>

<%= link_to '新規メッセージの投稿', new_message_path %>